let username = "alice";
let score = 100;
let flags = [true, false, true];

function add(a:number, b:number){
    return a+b;
}

console.log(add(5,6));

const user = {
    name: "Alice",
    age: 30,
    isAdmin: true
};

console.log(user.name);